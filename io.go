package datura

/*
Read reads the artifact from the given reader.
*/
func (artifact *Artifact) Read(p []byte) (n int, err error) {
	return artifact.Read(p)
}

/*
Write writes the artifact to the given writer.
*/
func (artifact *Artifact) Write(p []byte) (n int, err error) {
	return artifact.Write(p)
}
